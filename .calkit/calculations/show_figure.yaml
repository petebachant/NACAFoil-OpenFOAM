kind: figure-html
inputs:
  - name: profile
    dtype: str
    choices:
      - "0012"
    ui: select
  - name: angle_of_attack
    dtype: int
    min: 0
    max: 20
    ui: slider
output:
  - name: figure
    dtype: str
    description: HTML with figures in it.
template_path: figures/interactive.html
